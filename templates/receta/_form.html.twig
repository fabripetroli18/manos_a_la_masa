{{ form_start(form) }}
    {{ form_widget(form) }}
    {% if recetum.id != null %}
        {% if recetum.usuario == usuario %}
            <button class="btn">{{ button_label|default('Save') }}</button>
        {% else %}
            Esta receta no te pertenece
        {% endif %}
    {% else %}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {% endif %}   
    
{{ form_end(form) }}
