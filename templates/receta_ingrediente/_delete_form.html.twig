<form method="post" action="{{ path('receta_ingrediente_delete', {'id': recetaingrediente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recetaingrediente.id) }}">
    <button class="btn">Delete</button>
</form>
