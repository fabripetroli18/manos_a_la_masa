{% extends 'base.html.twig' %}

{% block title %}RecetaIngrediente{% endblock %}

{% block body %}
    <h1>RecetaIngrediente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ receta_ingrediente.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ receta_ingrediente.cantidad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('receta_ingrediente_index') }}">back to list</a>

    <a href="{{ path('receta_ingrediente_edit', {'id': receta_ingrediente.id}) }}">edit</a>

    {{ include('receta_ingrediente/_delete_form.html.twig') }}
{% endblock %}
