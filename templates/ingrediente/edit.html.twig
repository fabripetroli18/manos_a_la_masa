{% extends 'base.html.twig' %}

{% block title %}Edit Ingrediente{% endblock %}

{% block body %}
    <h1>Edit Ingrediente</h1>

    {{ include('ingrediente/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('ingrediente_index') }}">back to list</a>

    {{ include('ingrediente/_delete_form.html.twig') }}

    {% if existe is defined %}
    	{% if existe %}
    		No se puede eliminar, este ingrediente esta siendo utilizado
    	{% endif %}
    {% endif %}
{% endblock %}
